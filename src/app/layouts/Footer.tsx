/**
 * Layout footer — re-exported from the canonical shared Footer so both import
 * paths (via "@/app/layouts" and "@/shared/components") resolve to a single
 * implementation.
 */
export { Footer } from "@/shared/components/Footer/Footer";